# AI-BOT visual semantics connector

Publishes immutable task profiles, taxonomies, review policy and annotation-contract bindings. It contains no image bytes, provider credentials, mutable spend counters, database rows or runtime secrets.
