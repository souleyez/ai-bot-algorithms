# AI-BOT validation Source

Publishes one frozen, schema-validated snapshot from the append-only AI-BOT
evidence ledger. It reads only the loopback validation API and never scans
device, artifact, deployment or log directories.
