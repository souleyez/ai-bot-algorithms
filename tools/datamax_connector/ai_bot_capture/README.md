# AI-BOT raw capture connector

Publishes frozen raw-capture identity and content metadata only. It never emits human truth, training eligibility, model observations, or image bytes. Its credential, watermark, publication policy and acknowledgement state are separate from reviewed truth.
