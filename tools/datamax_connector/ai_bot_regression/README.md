# AI-BOT manual regression connector

Publishes exactly one immutable, owner-curated selection. There is no schedule or latest selector. The publisher defaults to status-only and requires both an explicit execution flag and a recorded approval reference before submitting an idempotent Source run.
