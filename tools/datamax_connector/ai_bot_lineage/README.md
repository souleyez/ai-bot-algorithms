# AI-BOT lineage Source

Publishes one frozen, schema-validated snapshot from the append-only AI-BOT
evidence ledger. It reads only the loopback lineage API and never scans model,
training, deployment or log directories.
